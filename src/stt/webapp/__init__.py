"""Phone-accessible web app: browse audio, transcribe (async), review transcripts.

Bound to 127.0.0.1 and exposed to the phone over Tailscale (see the repo's
docs/DEPLOY_TAILNET.md). No auth — the tailnet is the security boundary.
"""
